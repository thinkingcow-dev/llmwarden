---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.0
  name: llmaccesses.llmwarden.io
spec:
  group: llmwarden.io
  names:
    kind: LLMAccess
    listKind: LLMAccessList
    plural: llmaccesses
    shortNames:
    - llma
    singular: llmaccess
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.providerRef.name
      name: Provider
      type: string
    - jsonPath: .spec.secretName
      name: Secret
      type: string
    - jsonPath: .status.conditions[?(@.type=="Ready")].status
      name: Ready
      type: string
    - jsonPath: .status.lastRotation
      name: Last Rotation
      type: date
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          LLMAccess is the Schema for the llmaccesses API.
          It requests access to an LLM provider for a workload in a namespace.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of LLMAccess
            properties:
              injection:
                description: Injection defines how credentials are injected into matching
                  pods
                properties:
                  env:
                    description: Env defines environment variable injection
                    items:
                      description: EnvVarMapping defines mapping from secret key to
                        environment variable
                      properties:
                        name:
                          description: Name is the environment variable name to set
                            in the pod
                          minLength: 1
                          type: string
                        secretKey:
                          description: SecretKey is the key in the generated secret
                            to map from
                          minLength: 1
                          type: string
                      required:
                      - name
                      - secretKey
                      type: object
                    type: array
                  volume:
                    description: Volume defines volume mount injection
                    properties:
                      mountPath:
                        description: MountPath is where to mount the secret volume
                          in the pod
                        minLength: 1
                        type: string
                      readOnly:
                        default: true
                        description: ReadOnly determines if the volume should be mounted
                          read-only
                        type: boolean
                    required:
                    - mountPath
                    type: object
                type: object
              models:
                description: |-
                  Models is a list of model names/IDs that this access requires.
                  Must be a subset of the provider's allowedModels.
                items:
                  type: string
                minItems: 1
                type: array
              providerRef:
                description: ProviderRef references the cluster-scoped LLMProvider
                  resource
                properties:
                  name:
                    description: Name of the LLMProvider resource
                    minLength: 1
                    type: string
                required:
                - name
                type: object
              rotation:
                description: |-
                  Rotation allows overriding the provider's rotation schedule
                  The interval must be less than or equal to the provider's interval
                properties:
                  interval:
                    description: |-
                      Interval is the duration between credential rotations (e.g., "7d", "24h")
                      Must be less than or equal to the provider's rotation interval
                    pattern: ^\d+[dhm]$
                    type: string
                type: object
              secretName:
                description: |-
                  SecretName is the name of the Kubernetes Secret to create in this namespace
                  containing the credentials
                minLength: 1
                type: string
              workloadSelector:
                description: WorkloadSelector determines which pods receive credential
                  injection via webhook
                properties:
                  matchExpressions:
                    description: matchExpressions is a list of label selector requirements.
                      The requirements are ANDed.
                    items:
                      description: |-
                        A label selector requirement is a selector that contains values, a key, and an operator that
                        relates the key and values.
                      properties:
                        key:
                          description: key is the label key that the selector applies
                            to.
                          type: string
                        operator:
                          description: |-
                            operator represents a key's relationship to a set of values.
                            Valid operators are In, NotIn, Exists and DoesNotExist.
                          type: string
                        values:
                          description: |-
                            values is an array of string values. If the operator is In or NotIn,
                            the values array must be non-empty. If the operator is Exists or DoesNotExist,
                            the values array must be empty. This array is replaced during a strategic
                            merge patch.
                          items:
                            type: string
                          type: array
                          x-kubernetes-list-type: atomic
                      required:
                      - key
                      - operator
                      type: object
                    type: array
                    x-kubernetes-list-type: atomic
                  matchLabels:
                    additionalProperties:
                      type: string
                    description: |-
                      matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                      map is equivalent to an element of matchExpressions, whose key field is "key", the
                      operator is "In", and the values array contains only "value". The requirements are ANDed.
                    type: object
                type: object
                x-kubernetes-map-type: atomic
            required:
            - injection
            - providerRef
            - secretName
            type: object
          status:
            description: status defines the observed state of LLMAccess
            properties:
              conditions:
                description: Conditions represent the current state of the LLMAccess
                  resource
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              lastRotation:
                description: LastRotation is the timestamp of the last credential
                  rotation
                format: date-time
                type: string
              nextRotation:
                description: NextRotation is the timestamp of the next scheduled rotation
                format: date-time
                type: string
              provisionedModels:
                description: ProvisionedModels is the list of models that have been
                  successfully provisioned
                items:
                  type: string
                type: array
              secretRef:
                description: SecretRef references the created Secret containing credentials
                properties:
                  apiVersion:
                    description: API version of the referent.
                    type: string
                  fieldPath:
                    description: |-
                      If referring to a piece of an object instead of an entire object, this string
                      should contain a valid JSON/Go field access statement, such as desiredState.manifest.containers[2].
                      For example, if the object reference is to a container within a pod, this would take on a value like:
                      "spec.containers{name}" (where "name" refers to the name of the container that triggered
                      the event) or if no container name is specified "spec.containers[2]" (container with
                      index 2 in this pod). This syntax is chosen only to have some well-defined way of
                      referencing a part of an object.
                    type: string
                  kind:
                    description: |-
                      Kind of the referent.
                      More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
                    type: string
                  name:
                    description: |-
                      Name of the referent.
                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                    type: string
                  namespace:
                    description: |-
                      Namespace of the referent.
                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/namespaces/
                    type: string
                  resourceVersion:
                    description: |-
                      Specific resourceVersion to which this reference is made, if any.
                      More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#concurrency-control-and-consistency
                    type: string
                  uid:
                    description: |-
                      UID of the referent.
                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#uids
                    type: string
                type: object
                x-kubernetes-map-type: atomic
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
